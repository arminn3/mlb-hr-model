pybaseball>=2.2.5
pandas>=1.5.0
numpy>=1.23.0
requests>=2.28.0
thefuzz[speedup]>=0.19.0
python-dotenv>=1.0.0
tabulate>=0.9.0

# NFL side (Phase 1+) — nflverse data. Local dev is py3.9 so nfl_data_py
# (pandas) rather than nflreadpy (needs py3.10+/polars).
nfl_data_py
